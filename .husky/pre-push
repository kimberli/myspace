. "$(dirname -- "$0")/_/husky.sh"

declare PHOTOS_CHANGED=$(git show --name-only -r --stat --oneline HEAD^^..HEAD | grep 'photos/')

npm run lint
npm run build

if [[ $PHOTOS_CHANGED ]]; then scripts/organize_photos.sh -c; fi
